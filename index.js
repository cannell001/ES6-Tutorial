window.onload = function(){

    const pi = 3.142;

    function calcArea(radius){
        const pi = 10
        console.log(`The area is ${pi * radius * radius}`);
    }

    console.log(pi);
    calcArea(5);
}
